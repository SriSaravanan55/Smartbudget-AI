fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
anthropic==0.34.2
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# Optional: enables the investment-tips RAG feature. Requires a C++ compiler
# to build on Windows (Microsoft C++ Build Tools). The app runs fully without
# it -- investment guidance just uses static fallback tips instead.
# chromadb==0.5.5

# Testing (dev only -- not needed to run the app)
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
